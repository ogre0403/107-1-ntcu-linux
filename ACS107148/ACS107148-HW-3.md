# HW-3
## 第一題
* 用 useradd 指令新增 examuser1 examuser2 examuser3 三個帳號，並用 passwd 更改帳號密碼。

![](https://i.imgur.com/meZcIFY.png)
* 用 userdel -r examuser3 刪除 examuser3 並把家目錄裡的郵件檔案刪除。

![](https://i.imgur.com/VTx02cr.png)
* 用 userdel 刪除examuser 留下相關的郵件檔案。

![](https://i.imgur.com/y6hQPkf.png)
* 用 useradd -u 1001 -U examuser1 重建原本的帳號，因為被刪除的 examuser1 的 UID 是 1001 ，"-u"可指定UID，"-U"可建立和帳號相同的群組。

![](https://i.imgur.com/RTFjsLb.png)

## 第二題
*　創建　examuser4 ，密碼: itisexam 。

![](https://i.imgur.com/EySoado.png)
* 使用 cp 指令，將 /etc/securetty 複製給 examuser4，並且利用chmod指令設定權限。

![](https://i.imgur.com/Q0wbZPh.png)
* 建立 /examdata/change.txt 空檔案，並利用 chmod 指令設定 sshd 為擁有者，chgrp 指令設定群組為 users，chmod指令設定權限，最後再用touch -t 設定規定時間。

![](https://i.imgur.com/yDsfp5M.png)

## 第三題
* 請使用 root 的身份建立底下的檔案與權限：
    
    
        drwxrwxr-x  root root /dev/shm/unit05/
        drwxr-xr--  root root /dev/shm/unit05/dir1/
        -rw-r--r--  root root /dev/shm/unit05/dir1/file1 (複製來自     /etc/hosts)
        drwxr-x--x  root root /dev/shm/unit05/dir2/
        -rw-r--r--  root root /dev/shm/unit05/dir2/file2 (複製來自     /etc/hosts)
        drwxr-xr-x  root root /dev/shm/unit05/dir3/
        -rw-rw-rw-  root root /dev/shm/unit05/dir3/file3 (複製來自     /etc/hosts)
        drwxrwxrwx  root root /dev/shm/unit05/dir4/
        -rw-------  root root /dev/shm/unit05/dir4/file4 (複製來自     /etc/hosts)

* 利用mkdir指令創建目錄之後，再利用cp指令複製file1~4，創建及複製途中順便利用chmod指令設定規定的權限。

![](https://i.imgur.com/klLIZY2.png)
![](https://i.imgur.com/oZBkxyC.png)
* dir1在other權限上是r--，代表一般使用者只能查看，dir2在other權限上是--x，代表一般使用者不能查看但可以執行，dir3在other權限上是r-x，代表一般使用者可以查看執行，但不能修改，dir4在other權限上是rwx，代表一般使用者可以查看以及讀寫。

![](https://i.imgur.com/Dl31YnN.png)
* file1即使other能查看，但他的上一層dir1是不可執行，所以整體而言無法執行，file2的上一層dir2可執行，而本身只有r--所以只能查看，file3的上一層dir3可執行，故依照權限此使用者可讀可寫，file4的上一層dir4可執行，但本身沒有任何權限。

![](https://i.imgur.com/kM5sZEH.png)
* file1無法儲存，因為在other權限上沒有修改權限。

![](https:/![](https://i.imgur.com/CzhtyiM.png)
* file2無法儲存，因為在other權限上沒有修改權限。

![](https://i.imgur.com/t0jAzre.png)
* file3可以儲存，因為在other權限上有修改權限。

![](https://i.imgur.com/TvRP4m2.png)
* file4無法儲存，因為在other權限上沒有修改權限。

![](https://i.imgur.com/1C0u0VZ.png)








